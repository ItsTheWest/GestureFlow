[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gestureflow"
version = "0.1.0"
description = "Real-time hand gesture recognition pipeline"
requires-python = ">=3.10"

[tool.setuptools]
py-modules = ["utils"]
