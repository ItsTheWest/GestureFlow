# GestureFlow — dependencias verificadas con Python 3.14.5 (jun 2026)
#
# Recrear el entorno:
#   rm -rf venv
#   python -m venv venv
#   ./venv/bin/pip install -r requirements.txt
#
# OpenCV (import cv2) se instala solo via mediapipe → opencv-contrib-python.
# No agregar opencv-python: instala dos paquetes OpenCV y puede romper imports.

mediapipe==0.10.35
numpy==1.26.4
scikit-learn==1.9.0
tensorflow==2.16.1
pynput>=1.7.7
-e .
